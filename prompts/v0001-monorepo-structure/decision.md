# Decisão

O repositório será dividido em `frontend/` e `backend/`. O backend será
organizado inicialmente em Python, com áreas separadas para configuração,
modelos, serviços, utilitários, testes e integrações futuras.

Esta versão não define implementação funcional nem provedor externo.
